{
  "$for": {
    "b": "$.store.book[*]"
  },
  "$where": {
    "$lt": [
      "$b.price",
      10
    ]
  },
  "$orderby": "$b.price",
  "$return": {
    "title": "$b.title",
    "price": "$b.price"
  }
}
